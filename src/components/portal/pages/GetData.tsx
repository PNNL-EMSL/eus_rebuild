import React from 'react';
import RestrictedPage from 'components/shared/pages/RestrictedPage';

export default class GetData extends RestrictedPage {
    renderPage() {
        return (
            <div> TODO: Add Get Data Contents</div>
        );
    }
}